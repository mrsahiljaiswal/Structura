/**
 * Purpose: Color Design Tokens for Structura
 * Supports: Dark, Light, System, and High Contrast modes.
 */

export const colors = {
  // Base background and text colors mapped to CSS variables
  background: "var(--background)",
  foreground: "var(--foreground)",

  // Surface and card colors
  card: "var(--card)",
  cardForeground: "var(--card-foreground)",
  popover: "var(--popover)",
  popoverForeground: "var(--popover-foreground)",

  // Core brand accents
  primary: "var(--primary)",
  primaryForeground: "var(--primary-foreground)",
  secondary: "var(--secondary)",
  secondaryForeground: "var(--secondary-foreground)",

  // Muted states
  muted: "var(--muted)",
  mutedForeground: "var(--muted-foreground)",

  // Interactive accents and state glows
  accent: "var(--accent)",
  accentForeground: "var(--accent-foreground)",
  destructive: "var(--destructive)",
  destructiveForeground: "var(--destructive-foreground)",

  // Border configurations
  border: "var(--border)",
  input: "var(--input)",
  ring: "var(--ring)",

  // Chart data colors
  chart1: "var(--chart-1)",
  chart2: "var(--chart-2)",
  chart3: "var(--chart-3)",
  chart4: "var(--chart-4)",
  chart5: "var(--chart-5)",

  // Sidebar specific tokens
  sidebar: "var(--sidebar)",
  sidebarForeground: "var(--sidebar-foreground)",
  sidebarPrimary: "var(--sidebar-primary)",
  sidebarPrimaryForeground: "var(--sidebar-primary-foreground)",
  sidebarAccent: "var(--sidebar-accent)",
  sidebarAccentForeground: "var(--sidebar-accent-foreground)",
  sidebarBorder: "var(--sidebar-border)",
  sidebarRing: "var(--sidebar-ring)",
};
