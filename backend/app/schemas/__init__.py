# Pydantic request/response schemas live here (one module per domain
# entity, mirroring app/models). Kept separate from ORM models so API
# contracts can evolve independently of the database schema.
#
# No schemas are defined yet -- this project is configuration-only.
